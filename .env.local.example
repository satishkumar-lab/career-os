# Supabase — Supabase Dashboard → Settings → API
#
# Use the PROJECT URL only (Project URL / API URL base):
#   ✅ https://your-project-ref.supabase.co
#   ❌ https://your-project-ref.supabase.co/rest/v1   (REST endpoint — breaks OAuth)
#
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
#
# Optional server-only fallbacks (same values as above):
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
#
# On Vercel: set NEXT_PUBLIC_* vars, then redeploy so client bundles pick them up.
